rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Menu item photos: public read, admin write only
    match /menu-items/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.admin == true;
    }

    // Everything else: deny by default
  }
}
